export interface reviewLikeInfo{
    mittente: string,
    destinatario: string,
    filmId : number
}

export interface reviewReportInfo{
    mittente: string,
    destinatario: string,
    filmId : number,
    stato : string
}